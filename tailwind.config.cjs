const config = {
    content: ["./src/**/*.{html,js,svelte,ts}"],

    theme: {
        extend: {
            colors: {
                'blue-main': '#33ACE3'
            }
        },
    },

    plugins: [],
};

module.exports = config;
